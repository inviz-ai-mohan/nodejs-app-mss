name: CICD with node.ja application


on:  -----------> listin for perticuler  events 
   push:
       branches: [ master, main ]
  pull_request:
       branches: [ master,main ]

   schedule:
   - cron 0 12 * * 1 -----> schewdule the actions
  schedule events.

 Runs-on: ${{ matrix.os }}
 strategy:
    matrix:
         node-version: [8.x, 10.x, 12.x]
         os: [ ubuntu-latest ]

steps:
- uses: actions/checkout@v2
- name: Use node.js  ${{ matrix.node-version }} ---------> the run pre-existing actions 
   uses: actions/setup-node@v1
   with:
       node-version: ${{ matrix.node-version }}

- name: npm install build
   run: |
       npm ci
       npm run build –if-present -----> shell scripts
       npm test
 env:
   CI: true
